import { DnDDirective } from './dn-d.directive';

describe('DnDDirective', () => {
  it('should create an instance', () => {
    const directive = new DnDDirective();
    expect(directive).toBeTruthy();
  });
});
